# Core 设计

## 文档定位

本文档用于描述 `@rag-sdk/core` 的子包设计。

当前只定义该子包在目标架构中的职责边界，具体接口和实现设计后续在本文档内继续补充。

## 职责

`core` 是 SDK 的基础层，负责定义跨子包共享的核心数据结构、基础类型和基础协议。

`core` 应保持稳定、克制，不依赖其他 SDK 子包。

## 不负责

`core` 不负责索引流程编排。

`core` 不负责检索流程编排。

`core` 不负责外部能力适配。

`core` 不承载业务规则或业务流程。
