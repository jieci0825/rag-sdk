{
    "name": "rag-sdk",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "build": "tsdown",
        "clean": "rm -rf packages/*/dist",
        "typecheck": "tsc --noEmit",
        "test": "vitest run"
    },
    "devDependencies": {
        "@types/node": "^25.9.1",
        "prettier": "^3.8.3",
        "tsdown": "^0.22.0",
        "typescript": "^6.0.3",
        "unrun": "^0.3.0",
        "vitest": "^4.1.7"
    }
}
