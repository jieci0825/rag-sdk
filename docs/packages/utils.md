# Utils 设计

## 文档定位

本文档用于描述 `@rag-sdk/utils` 的子包设计。

当前只定义该子包在目标架构中的职责边界，具体接口和实现设计后续在本文档内继续补充。

## 职责

`utils` 负责提供跨子包复用的内部通用工具。

这些工具应当没有业务语义，也不绑定具体 RAG 流程阶段。

## 不负责

`utils` 不定义核心数据结构。

`utils` 不定义索引或检索协议。

`utils` 不承载业务逻辑。

`utils` 不作为对外能力扩展入口。
