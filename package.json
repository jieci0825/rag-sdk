{
    "name": "rag-sdk",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "build": "pnpm -r build",
        "clean": "pnpm -r clean"
    },
    "devDependencies": {
        "prettier": "^3.8.3",
        "typescript": "^6.0.3",
        "vitest": "^4.1.7"
    }
}
